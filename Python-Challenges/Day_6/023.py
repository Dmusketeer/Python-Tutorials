# Ask the user to enter an integer that is over 500. Work 
# out the square root of that number and display it to two
# decimal places. 
import math
UserInput=int(input("Enter an integer that is over 500: "))
print(round(math.sqrt(UserInput),2))